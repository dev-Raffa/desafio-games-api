import { Button } from '../buttons';
import { Text } from '../typography/texts';
import { Title } from '../typography/title';
import { FieldForm } from './parts/field';
import { FormWrap } from './parts/wrap';

export const Form = {
  wrap: FormWrap,
  title: Title,
  text: Text,
  field: FieldForm,
  button: Button
};
