import { Text } from '@/components/typography/texts';
import { Icon } from '@/components/medias/icons';
import { StyledButtonWrap } from './styles';
import { TextLink } from '../typography/links';

export const Button = {
  wrap: StyledButtonWrap,
  text: Text,
  link: TextLink,
  icon: Icon
};
